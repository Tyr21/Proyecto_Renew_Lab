# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules

# Tauri / Rust: artefactos (Cargo.lock y package-lock.json SÍ se versionan)
src-tauri/target/

# Cargo (directorio de compilación de tests en la raíz; no versionar)
target-test-db/

dist
dist-ssr
*.local

# SQLite database backups
*.db

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Certificados locales / merge efímero de firma en CI (no commitear)
/certificate/
src-tauri/tauri-signing.ci.json
src-tauri/tauri-release.ci.json

# Claves minisign del updater (solo la pública va en tauri.conf.json versionado)
src-tauri/.updater-key.local
src-tauri/.updater-key.local.pub
